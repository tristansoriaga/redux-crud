import React from "react";
import "./Header.css";

import Nav from "../components/Nav";

const Header = () => {
  return (
    <div class="header">
      <Nav />
    </div>
  );
};

export default Header;
